options {
    directory  "/var/named";                       //地址库默认存放位置
};
zone  "fu.cn" {                               //定义正向DNS区域
    type  master;                                  //主区域
    file  "fu.cn.zone";                       //自定义地址库文件名
};
